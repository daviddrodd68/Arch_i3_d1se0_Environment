import React from 'react';
import { {{#each imports}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } from 'react-native';
import { StyleSheet } from 'react-native';
{{#if navigation.hasNavigation}}
import { useNavigation } from '@react-navigation/native';
{{/if}}
{{#if designTokens}}
import { theme } from '../theme';
{{/if}}

{{#if navigation.hasNavigation}}
interface {{screenName}}Props {
  navigation: any;
  route: any;
}
{{/if}}

export const {{screenName}}: React.FC<{{#if navigation.hasNavigation}}{{screenName}}Props{{else}}{}{{/if}}> = ({{#if navigation.hasNavigation}}{ navigation, route }{{/if}}) => {
{{#if navigation.hasNavigation}}
  // const navigation = useNavigation();
{{/if}}

  return (
{{{jsx}}}
  );
};

const styles = StyleSheet.create({
{{#each styles}}
  {{@key}}: {
{{#each this}}
    {{@key}}: {{#if (eq @key 'shadowColor')}}'{{{this}}}'{{else if (eq @key 'backgroundColor')}}'{{{this}}}'{{else if (eq @key 'borderColor')}}'{{{this}}}'{{else if (eq @key 'color')}}'{{{this}}}'{{else}}{{this}}{{/if}},
{{/each}}
  },
{{/each}}
});

export default {{screenName}};
